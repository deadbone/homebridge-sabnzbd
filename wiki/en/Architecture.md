# Architecture

The plugin is a Homebridge dynamic platform. It creates one cached SABnzbd accessory and updates all services from a shared snapshot built from queue, history and status API responses.
