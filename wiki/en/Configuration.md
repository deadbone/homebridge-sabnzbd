# Configuration

Required values:

- `url`: the SABnzbd web interface URL.
- `apiKey`: the SABnzbd API key from Config > General.

Optional values control timeout, refresh interval, temporary pause duration, preset speed limit, recent failure window and whether the clear warnings switch is exposed.
